# Security Policy

## 🔒 我们的安全承诺

Castorice Agent 是一个**在你自己机器上运行**的本地优先（local-first）智能体框架，数据主权和隐私是设计前提。我们非常重视任何可能影响以下目标的安全问题：

- **Personastore 数据不被未授权读取 / 导出**
- **API Key / 凭据不被明文泄露（日志、序列化、广播网络）**
- **自主循环 / 成本闸 / 熔断器不被绕过导致意外费用**
- **HTTP 服务不被越权调用**
- **Tauri 桌面端 / Docker 镜像不被植入恶意内容**

---

## ✅ 受支持版本

我们只会针对以下版本接收和修复安全漏洞：

| 版本 | 支持状态 | 说明 |
|------|----------|------|
| `main` 分支 | ✅ 完全支持 | 所有安全修复优先合入主线 |
| `v3.x`（最新 minor release） | ✅ 支持 | 重大漏洞会在修复后打新 patch tag |
| `v2.x` 及更早 | ❌ 不再支持 | 请尽快升级至 v3 |

---

## 🚨 如何报告漏洞

### ⚠️ 请不要直接开公开 Issue

如果发现的是**潜在可被利用**的漏洞（密钥泄露、RCE 绕过、鉴权缺失、权限提升等），**请不要在公开 Issue、PR、讨论区或社交平台披露**。公开披露等于给所有使用者同时打开了攻击窗口。

### 推荐渠道（按优先级排序）

1. **GitHub 私有安全报告（推荐，全程加密 + 可协作）**
   - 仓库页面 → `Security` → `Advisories` → `Report a vulnerability`
   - 提交后你会自动进入私有讨论线程，所有回复和补丁都不会被公开看到
   - 我们会在 **48 小时内** 确认收到，**7 天内** 给出修复或接受/拒绝的结论

2. **邮件报告**
   - 收件人：**lixiao@acyam.top**
   - 主题格式：`[Castorice-SEC] <一句话描述漏洞>`
   - 如果内容敏感，建议在正文中附上你的 GitHub 用户名，我们会切换到私有 Advisory 继续跟进
   - **建议包含的信息**：复现步骤（PoC）、受影响版本、期望行为 vs 实际行为、你的环境（OS / Python 版本 / 运行模式：cli / http / tauri / docker）

---

## 🔍 什么算漏洞 / 什么不算

### ✅ 通常被视为漏洞

- API Key、访问令牌、数据库路径等**敏感信息被写入普通日志 / stdout / 导出文件**
- Personastore 文件权限过宽（如 `mode 777` 写入），或导出文件包含**明文密钥**
- HTTP 服务（`--mode http`）下：
  - 没有鉴权时被**跨域 / CSRF** 触发危险操作（执行终端命令、写文件、删记忆）
  - 成本闸、熔断器能通过伪造请求**绕过**
- Docker / Tauri 镜像分发链路中：
  - 镜像 layer 内有**开发者本地密钥**
  - 启动脚本执行了**未签名的远程代码**（无校验下载依赖）
- 自主循环（Agent Loop）中：
  - 恶意广播 payload 能**注入记忆 / 篡改自我概念**而不被信任边界拦截
  - 成本闸超发后**没有停止**（实际费用 > 成本闸设定 × 1.5）

### ❌ 通常不被视为漏洞（是你掌控的特性本身）

Castorice Agent 的设计允许它**在你授权的前提下**执行任意本地命令、调用付费 API、读写文件。这些能力本身不是漏洞：

- 你**显式配置了终端执行能力**后，Agent 按指令执行 `rm -rf` / 删库 / 调用付费接口
- 你**提供了高权限 API Key**，Agent 正常使用该 Key 产生费用
- 你**在非本机环境开放了 HTTP 端口**却没有加反向代理鉴权 —— 这属于部署配置责任
- Docker 容器被你自己 `--privileged` / `-v /:/host` 挂载后出现的权限问题

简言之：**Castorice Agent 不是沙箱，它是你机器上的一个程序。你赋予它多大权限，它就能干多大事。** 安全模型的核心假设是"运行环境本身可信 + 使用者按文档配置权限"。

---

## 🛡 Castorice Agent 的内置安全机制

| 机制 | 作用 |
|------|------|
| **Personastore 本地存储** | 所有记忆、密钥、人格数据默认只写入你机器上的本地目录，不向云端同步 |
| **零遥测** | 不收集、不上传任何使用数据（无 usage ping、无错误上报） |
| **成本闸（Cost Gate）** | 每次 LLM 调用累计开销，超阈值立即熔断请求 |
| **熔断器（Circuit Breaker）** | 单位时间 LLM 调用 / 工具执行次数超限后自动冷却 |
| **供应商 API Key 独立存储** | 每个供应商凭据独立配置，不会出现在记忆检索结果里 |
| **信任边界（EigenFlux 网络）** | 来自其他 Agent 的广播内容不直接写入自传式记忆，会先经过亲密度 + 意图评估 |
| **Docker 加固** | 官方镜像默认读-写分离数据卷、非 root 运行（如果使用 docker-compose） |
| **.gitignore / .env.example** | 默认忽略 `.env`、Personastore、venv、构建产物等本地敏感文件 |

如果以上任一机制被发现可被绕过，请按上面的渠道上报，我们会把它当作**高优先级问题**处理。

---

## 📌 披露政策（Coordinated Disclosure）

我们遵循**协调披露**流程：

1. **你报告漏洞** → 我们 48h 内确认
2. **确认有效后** → 进入 14~90 天修复窗口（按严重程度定）
3. **修复合入 + 打 Release** → 同时在 GitHub Advisory 发公开公告，**感谢并署名你的报告**（除非你要求匿名）
4. 在公告中给出**受影响版本范围**、**修复版本**、**用户需采取的行动**

我们反对 0-day 公开披露，也不会对合理渠道报告漏洞的个人采取任何法律行动。

---

## 📮 联系

- 安全漏洞报告邮箱：**lixiao@acyam.top**
- 非安全的一般 Bug / 讨论：使用仓库 Issue
